import { styled } from '@storybook/theming';

const Card = styled.div({
  backgroundColor: '#fff',
  borderRadius: '1em',
  boxShadow: '0 2em 4em rgba(0, 0, 0, .2)',
  display: 'inline-block',
  fontFamily: 'sans-serif',
  lineHeight: 1.5,
  padding: '4em',
});

export default Card;
